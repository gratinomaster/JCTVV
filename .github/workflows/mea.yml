name: Run OpenCode

on:
  workflow_dispatch:
  push:

jobs:
  opencode:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install OpenCode
        run: |
          curl -fsSL https://opencode.ai/install | bash
          echo "$HOME/.opencode/bin" >> $GITHUB_PATH

      - name: Ask OpenCode
        run: |
          PERGUNTA=$(cat << 'EOF'
          o epg da lista1.m3u não esta funcionando, corrija tudo para que funcione corretamente, e o sportv tem que ter o tvg-id correto
          EOF
          )

          echo "Pergunta: $PERGUNTA"
          echo ""
          echo "Resposta do OpenCode:"
          opencode run "$PERGUNTA"

      - name: Commit changes
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          git add IRIB.m3u || true
          git commit -m "Arquivo IRIB.m3u gerado pelo OpenCode" || echo "Nada para commit"
          git push
