name: LIST

on:
  workflow_dispatch:

jobs:
  opencode:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg curl wget

      - name: Install OpenCode
        run: |
          curl -fsSL https://opencode.ai/install | bash

      - name: Add OpenCode to PATH
        run: |
          echo "$HOME/.opencode/bin" >> $GITHUB_PATH

      - name: Verify installation
        run: |
          opencode --version

      - name: Run OpenCode Prompt
        run: |
          opencode run "
          modifique GLOBO.py para que os canais já saiam com epg configurados, pois agora não tem epg nenhum, o .py deve gerar os canais com os epgs corretos, preciso de epg que funciona e os canais devem receber tvg-id corretos, abra o arquivo .xml ou .xml.gz para ver se o guia esta atualizado, coloque epgs locais para os canais que tiverem, para que não usem obrigatoriamente o epg nacional, para os que puderem ter epg regional, não quero que o proprio epg esteja dentro do .py, encontre algum epg na internet que tenha os guias locais
          "

      - name: Save result
        run: |
          ls -la

      - name: Commit result (optional)
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add .
          git commit -m "Updated IPTV list" || echo "No changes"
          git push
